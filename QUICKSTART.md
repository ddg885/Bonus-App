# QUICKSTART

```bash
npm run dev
```
Open `http://localhost:4173`.

## Quick demo
- Use seeded data already loaded on startup.
- Optional: upload files from `/sample-data` on the **POM Inputs** page.
- Navigate pages from the left sidebar.
